version: "1.5"
namespace:
    key: default
    name: Default
flags:
    - key: show_faq
      name: show_faq
      type: BOOLEAN_FLAG_TYPE
      enabled: true
      metadata:
        faq: Only for new users
